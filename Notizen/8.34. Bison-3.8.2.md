## 📦 Bison (Parser Generator)

---

### 1. Paketfunktion

- **Zweck:** Enthält einen **Parser-Generator**.
    
- **Kern:** Analysiert die **grammatikalische Struktur** von Textdateien (z.B. Quellcode) basierend auf einer Regelmenge (Grammatik).
    
- **Technik:** Generiert das Programm, das die Analyse durchführt (den sogenannten **Parser**).
    
- **Relevanz:** Essentielles **Entwickler-Tool** und **Toolchain-Komponente** (ersetzt Yacc) für den Bau von Compilern und Shells (wie Bash).
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration mit explizitem Pfad für die Dokumentation.
    
    ```
    ./configure --prefix=/usr --docdir=/usr/share/doc/bison-3.8.2
    ```
    
- **Kompilierung:**
    
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):** Ausführung der Tests.
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Automatischer Wrapper:** `bison` installiert automatisch den Wrapper **`yacc`** (`Yet Another Compiler Compiler`).
    
- **Grund:** Stellt Kompatibilität für ältere Build-Skripte sicher, die noch explizit `yacc` aufrufen.
    

---

### 4. Essenzielle Programme und ihre Nutzung

| Programm            | Zweck (Crème de la Crème)                                               | Ziel-Bezug (Systemverständnis / Core-Tools)                                                                            |
| ------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **`bison`**         | Der Haupt-Parser-Generator.                                             | **Core-Tool:** Transformiert eine **`.y`- oder `.bison`-Grammatikdatei** in ausführbaren C-Code (Parser).              |
| **`bison datei.y`** | Generiert `datei.tab.c` und `datei.tab.h` (Der generierte Parser-Code). | **Systemverständnis:** Grundlegendes Verständnis des Kompilierungs-Workflows (Lexing/Parsing).                         |
| **`yacc`**          | Ein **Wrapper-Skript** (Symbolischer Link).                             | **Troubleshooting/FHS:** Stellt sicher, dass Legacy-Build-Systeme, die `yacc` erwarten, erfolgreich `bison` verwenden. |
| **`liby.a`**        | Die Yacc-Bibliothek.                                                    | **Systemverständnis:** Enthält Implementierungen für Yacc-kompatible Funktionen (z.B. `yyerror`).                      |
