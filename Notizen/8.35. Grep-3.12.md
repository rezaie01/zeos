## 📦 Grep (Global Regular Expression Print)

---

### 1. Paketfunktion

- **Zweck:** Enthält Programme zum **Durchsuchen des Inhalts von Dateien**.
    
- **Kern:** Findet Zeilen in Textdateien, die einem **regulären Ausdruck (Regular Expression)** entsprechen.
    
- **Relevanz:** Essentielles **Core-Tool** für Systemdiagnose, **Troubleshooting** und die Datenfilterung in Bash-Skripten.
    

---

### 2. Kompilierungsprozess im Überblick

- **Patch (Kritisch für Tests):** Entfernung einer Warnung über die veralteten Befehle `egrep` und `fgrep`, um fehlschlagende Tests in nachfolgenden Paketen zu verhindern.
    ```
    sed -i "s/echo/#echo/" src/egrep.sh
    ```
    
- **Konfiguration:** Standardkonfiguration.

    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung:**
   
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):**
   
    ```
    make check
    ```
    
- **Installation:**
   
    ```
    make install
    ```
    

---

### 3. Essenzielle Programme und ihre Nutzung

|**Programm**|**Zweck (Crème de la Crème)**|**Ziel-Bezug (Troubleshooting / Bash-Fähigkeiten)**|
|---|---|---|
|**`grep`**|Sucht nach Zeilen, die einem **Basic Regular Expression (BRE)** entsprechen.|**Core-Tool** für das Filtern von Logs und Konfigurationsdateien.|
|**`grep -E MUSTER datei`**|Sucht mit **Extended Regular Expressions (ERE)** (entspricht dem veralteten `egrep`).|**Bash-Fähigkeiten:** Ermöglicht komplexere Muster (z.B. `|
|**`grep -F STRING datei`**|Sucht nach **festen Strings** (keine regulären Ausdrücke) (entspricht dem veralteten `fgrep`).|**Bash-Fähigkeiten:** Schnellste Methode für die Suche nach exaktem Text.|
|**`grep -r MUSTER pfad`**|Sucht **rekursiv** (`-r`) in allen Dateien unterhalb eines Pfades.|**Troubleshooting:** Durchsuchen des gesamten Systems oder von Log-Verzeichnissen nach Fehlern.|
|**`grep -v MUSTER datei`**|**Invertiert** die Suche (`-v`) und gibt **alle Zeilen aus, die NICHT** dem Muster entsprechen.|**Troubleshooting:** Filtern von uninteressanten Statusmeldungen aus Log-Dateien.|
|**`grep -A 3 -B 2 MUSTER datei`**|Zeigt 3 Zeilen **nach** (`-A`) und 2 Zeilen **vor** (`-B`) dem Treffer an (Context).|**Troubleshooting:** Kontextanalyse von Fehlern in Log-Dateien.|
|**`grep -q MUSTER datei`**|**Stille Suche** (`-q`), gibt nichts aus, setzt nur den Exit-Code.|**Bash-Fähigkeiten:** Wird in Skripten für **Bedingungen** (`if grep -q ...`) verwendet.|