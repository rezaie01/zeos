#Diffutils #diff #cmp #diff3 #sdiff

## 🛠️ Diffutils-3.12: Lernnotizen für LFS

### 1. Paketfunktion

- Das Diffutils-Paket enthält Programme, die die **Unterschiede zwischen Dateien oder Verzeichnissen** anzeigen1.
    

---

### 2. Kompilierungsprozess

#### **Vorbereitung**

- Konfiguration des Pakets für die Installation im `/usr`-Prefix:
    ```
    ./configure --prefix=/usr
    ```

#### **Kompilierung und Test**

- Kompilierung:
    ```
    make
    ```
- Testdurchführung:
    ```
    make check
    ```

#### **Installation**

- Installation des Pakets:
    ```
    make install
    ```

---

### 3. Nach-der-Installation-Konfigurationen

- Es sind keine Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

|**Programm**|**Beschreibung**|**Nutzung (Fokus: Systemintegration)**|
|---|---|---|
|#cmp|**Vergleicht zwei Dateien Byte für Byte** und meldet Unterschiede6.|**Binär-Prüfung:** Nützlich, um schnell festzustellen, _ob_ sich zwei Dateien (insb. Binärdateien) unterscheiden, ohne die Unterschiede im Detail zu sehen. `cmp file1 file2`|
|#diff|**Vergleicht zwei Dateien oder Verzeichnisse Zeile für Zeile** und meldet, welche Zeilen sich unterscheiden7.|**Das Kernwerkzeug zur Konfigurations- und Quellcode-Verwaltung.**<br><br>  <br>  <br><br>**1. Konfigurationsvergleich:** `diff /etc/file.conf.original /etc/file.conf`<br><br>  <br><br>Zeigt Änderungen an Systemkonfigurationen an.<br><br>  <br>  <br><br>**2. Patch-Erstellung (Unified Format):** `diff -u <original.c> <modified.c> > patch.diff`<br><br>  <br><br>Das `-u` (Unified) Format ist Standard für die Erstellung von Patches, die mit dem `patch`-Befehl (aus einem anderen Paket) angewendet werden können.<br><br>  <br>  <br><br>**3. Verzeichnisvergleich:** `diff -r <dir1> <dir2>`<br><br>  <br><br>Vergleicht rekursiv den Inhalt zweier Verzeichnisse (z.B. Backup vs. Live-System).|
|#diff3|**Vergleicht drei Dateien Zeile für Zeile**8.|**Merge-Vorbereitung:** Wird seltener direkt verwendet, aber nützlich in Versionskontrollszenarien, um Konflikte zwischen drei Versionen (Basis, Meine, Ihre) zu finden.|
|#sdiff|**Führt zwei Dateien zusammen (merged) und gibt das Ergebnis interaktiv aus**9.|**Side-by-Side-Vergleich:** `sdiff -o <outputfile> <file1> <file2>`<br><br>  <br><br>Zeigt beide Dateien nebeneinander an und erlaubt eine interaktive Auswahl der zu übernehmenden Zeilen.|