## 📦 GDBM (GNU Database Manager)

---

### 1. Paketfunktion

- **Zweck:** Enthält den GNU Database Manager.
    
- **Kern:** Eine Bibliothek von Datenbankfunktionen, die **erweiterbares Hashing** verwendet.
    
- **Funktion:** Bietet Routinen zum Speichern, Suchen, Abrufen und Löschen von **Schlüssel/Daten-Paaren** in einer Datenbank.
    

---

### 2. Kompilierungsprozess im Überblick

- **Wichtige `configure`-Flags:**
    
    - `--disable-static`: Verhindert die Installation statischer Bibliotheken.
        
    - `--enable-libgdbm-compat`: Ermöglicht das Bauen der **Kompatibilitätsbibliothek** für ältere DBM-Routinen.
    ```
    ./configure --prefix=/usr \
        --disable-static \
        --enable-libgdbm-compat
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite:**
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- Keine spezifischen Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm/Bibliothek|Zweck|
|---|---|
|**`libgdbm.so`**|Enthält die Hauptfunktionen zur **Manipulation einer gehashten Datenbank** (API für Programme).|
|**`libgdbm_compat.so`**|Die **Kompatibilitätsbibliothek**, die ältere DBM-Funktionen für Legacy-Anwendungen bereitstellt.|
|**`gdbm_dump datei.db > dump.txt`**|**Exportiert** den Inhalt einer GDBM-Datenbank in eine Datei.|
|**`gdbm_load < dump.txt datei.db`**|**Erstellt** eine GDBM-Datenbank aus einer Export-Datei.|
|**`gdbmtool`**|Ein Dienstprogramm zum **Testen und Modifizieren** von GDBM-Datenbanken (Interaktion auf der Kommandozeile).|