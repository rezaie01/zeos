## 📦 Gettext (GNU Gettext)

---

### 1. Paketfunktion

- **Zweck:** Enthält Dienstprogramme für **Internationalisierung und Lokalisierung** (i18n/l10n).
    
- **Kern:** Ermöglicht Programmen die **NLS (Native Language Support)**, um Meldungen in der Muttersprache des Benutzers auszugeben.
    

---

### 2. Kompilierungsprozess im Überblick

- **Wichtige `configure`-Flags:**
    
    - `--disable-static`: Verhindert die Installation statischer Bibliotheken.
        
    - `--docdir=/usr/share/doc/gettext-0.26`: Setzt den korrekten Pfad für die Dokumentation.
        
    
    Bash
    
    ```
    ./configure --prefix=/usr \
        --disable-static \
        --docdir=/usr/share/doc/gettext-0.26
    ```
    
- **Kompilierung:**
    
    Bash
    
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):** Ausführung der Tests zur Sicherstellung der korrekten Funktion der Lokalisierungsroutinen.
    
    Bash
    
    ```
    make check
    ```
    
- **Installation:**
    
    Bash
    
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Berechtigungskorrektur (Sicherheit/Systemverständnis):** Korrigiert die Dateirechte für die _preloadable_ Bibliothek.
    
    Bash
    
    ```
    chmod -v 0755 /usr/lib/preloadable_libintl.so
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

| Programm         | Zweck (Crème de la Crème)                                                                                       | Ziel-Bezug (Systemverständnis / Workflow)                                                     |
| ---------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| **`xgettext`**   | **Extrahiert** übersetzbaren Text aus dem Quellcode. Erzeugt die **`.pot`-Template-Datei**.                     | **Karriere/Systemintegration:** Erster Schritt im Lokalisierungs-Workflow.                    |
| **`msgfmt`**     | **Kompiliert** die Text-Übersetzungsdatei (`.po`) in das **binäre Format (`.mo`)**.                             | **Systemverständnis:** `.mo` ist das Format, das das System zur Laufzeit liest.               |
| **`ngettext`**   | Zeigt Übersetzungen an, deren grammatikalische Form von einem numerischen Wert abhängt (z.B. **Pluralformen**). | **Systemverständnis:** Notwendig für sprachliche Komplexität (z.B. Russisch, Polnisch).       |
| **`envsubst`**   | **Ersetzt Umgebungsvariablen** in Shell-Format-Strings.                                                         | **Bash-Fähigkeiten:** Wird in Skripten zur Konfiguration und Variablensubstitution verwendet. |
| **`libintl.so`** | Die Laufzeitbibliothek, die die kompilierten `.mo`-Dateien lädt und für die Programmausgabe nutzt.              | **Systemverständnis:** Die Brücke zwischen Übersetzungsdateien und der Anwendung.             |