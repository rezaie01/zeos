## 📦 Sed (Stream Editor)

---

### 1. Paketfunktion

- **Zweck:** Enthält einen **Stream Editor**.
    
- **Kern:** Filtert und **transformiert** Textdateien in einem **einzigen Durchlauf** (Single Pass).
    
- **Relevanz:** Essentielles **Core-Tool** für Shell-Skripte und die **automatisierte Textverarbeitung** von Konfigurationsdateien während des LFS-Builds.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration ohne spezielle Optionen.
    
    Bash
    
    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung:** Kompilieren des Pakets und der HTML-Dokumentation.
    
    Bash
    
    ```
    make
    make html
    ```
    
- **Test-Suite (Wichtig für Troubleshooting):** Testlauf muss als nichtprivilegierter Benutzer (`tester`) erfolgen.
    
    Bash
    
    ```
    chown -R tester .
    su tester -c "PATH=$PATH make check"
    ```
    
- **Installation:** Installation des Programms und der Dokumentation.
    
    Bash
    
    ```
    make install
    install -d -m755 /usr/share/doc/sed-4.9
    install -m644 doc/sed.html /usr/share/doc/sed-4.9
    ```
    

---

### 3. Essenzielle Programme und ihre Nutzung

| Programm                               | Zweck (Crème de la Crème)                                                                       | Ziel-Bezug (Bash-Fähigkeiten)                                                           |
| -------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| **`sed`**                              | Der Stream Editor.                                                                              | **Core-Tool** für Textmanipulation.                                                     |
| **`sed 's/MUSTER/ERSATZ/g' datei`**    | **Suchen und Ersetzen** (`s/`) eines Musters durch einen Ersatz, **global** (`g`) in der Datei. | **Bash-Fähigkeiten:** Das wichtigste `sed`-Kommando für die Konfigurationsbearbeitung.  |
| **`sed '/MUSTER/d' datei`**            | Löscht (`d`) alle Zeilen, die dem **MUSTER** entsprechen.                                       | **Bash-Fähigkeiten:** Entfernen unerwünschter oder auskommentierter Zeilen in Skripten. |
| **`sed -i 's/MUSTER/ERSATZ/g' datei`** | Führt die Ersetzung **direkt in der Datei** (`-i` für _in-place_) durch.                        | **Bash-Fähigkeiten / Konfiguration:** Dauerhaftes Patchen von Konfigurationsdateien.    |
| **`sed '1,10d' datei`**                | Löscht die Zeilen 1 bis 10.                                                                     | **Bash-Fähigkeiten:** Gezielte Zeilenmanipulation.                                      |

**Auch sehe: [[sed]]**

