```bash
./configure --disable-shared
make
cp -v gettext-tools/src/{msgfmt,msgmerge,xgettext} /usr/bin
```
> **<font color="#9bbb59">ERKLÄRUNG</font>:**
>***`--disable-shared`***: brauchen wir zu diesem Zeitpunkt nicht die gemeinsam genutzte Gettext-Bibliotheken.

><font color="#ff0000">ERROR:</font>
>***<font color="#ff0000">`bash: ./configure: /bin/sh: bad interpreter: No such file or directory`</font>***
>
>***Ursache***: *`/bin/sh` ist zeigt zu dem `usr/bin/bash`, welche es wirklich nicht gibt.*
>***Lösung***: *Neue und richtige Symlink erstellen: * 
>`cd /usr/bin && rm sh && ln -sv bash sh`


><font color="#ff0000">ERROR:</font>
>***<font color="#ff0000">`./configure: line 3936: sed: command not found`</font>***

***Ursache***: `sed` zu compilieren habe ich vergessen
>***Lösung***: *`sed` neu compilieren* 
> 1.  `sudo chown --from root:root lfs -R $LFS/{etc,var,usr,tools,lib64}`
> 2. `su - lfs`
> 3. `cd $LFS/sources`
> 4. `tar -xf sed-4.9.tar.xz`
> 5. `cd sed-4.9`
> 6. `./configure --prefix=/usr --host=$LFS_TGT --build=$(./build-aux/config.guess)`
> 7. `make && make DESTDIR=$LFS install`
> 8. `cd .. && rm -rf sed-4.9`
> 9. `exit`
> 10. `sudo chown --from lfs root:root -R $LFS/{etc,var,usr,tools,lib64}`
> 11. Kontinuiere das Bauen von `Gettext`