## 📦 Automake (Makefile Generator)

---

### 1. Paketfunktion

- **Zweck:** Enthält Programme zur **automatischen Generierung von Makefiles** für die Verwendung mit Autoconf.
    
- **Kern:** Liest die Eingabedatei `Makefile.am` und erstellt daraus die Vorlagendatei `Makefile.in`.
    
- **Prinzip:** Standardisiert den Bauprozess von Software und sorgt für GNU-Standards-Konformität.
    

---

### 2. Kompilierungsprozess im Überblick

- **Wichtige `configure`-Flags:** Festlegung des Installationspfads und des Dokumentationspfads.
    ```
    ./configure --prefix=/usr --docdir=/usr/share/doc/automake-1.18.1
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite (Parallelisierung):** Die Tests werden parallelisiert ausgeführt, um interne Verzögerungen zu minimieren.
    ```
    make -j$((\$(nproc)>4?$(nproc):4)) check
    ```
    
- **Installation:**
    
    Bash
    
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- Keine zusätzlichen Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

| Programm           | Zweck (Crème de la Crème)                                                                                                | Nutzung (Extensiv für Systemintegration)                                                                                                                                            |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **#automake**      | **Makefile-Generator:** Erzeugt aus der Entwickler-Vorlage (`Makefile.am`) die generische Build-Vorlage (`Makefile.in`). | **`automake`**: Führt die Generierung aus, falls `Makefile.am` existiert.                                                                                                           |
|                    |                                                                                                                          | **`automake --add-missing`**: Kopiert häufig benötigte Hilfsskripte (wie `install-sh`, `missing`) in den Quellbaum, damit das Paket portabel wird.                                  |
|                    |                                                                                                                          | **`automake --foreign`**: Lockert die strengen GNU-Standards (z.B. fehlende `README` wird ignoriert) – nützlich beim Bauen von Software, die nicht primär für GNU entwickelt wurde. |
|                    |                                                                                                                          | **Struktur:** `Makefile.in` wird von `./configure` (generiert von Autoconf) in das finale, systemabhängige `Makefile` umgewandelt.                                                  |
| **#aclocal**       | **Makro-Aggregator:** Sammelt alle benötigten M4-Makros für Autoconf und schreibt sie in die Datei `aclocal.m4`.         | **`aclocal`**: Liest `configure.ac` und sucht nach verwendeten Makros (z.B. von Drittanbieterbibliotheken).                                                                         |
|                    |                                                                                                                          | **`aclocal -I m4`**: Sucht zusätzlich Makrodateien im Unterverzeichnis `m4/` des Quellbaums (für paketspezifische oder externe Makros).                                             |
|                    |                                                                                                                          | **Reihenfolge:** Muss vor `autoconf` ausgeführt werden, da `autoconf` die Datei `aclocal.m4` als Input benötigt.                                                                    |
| **#aclocal-1.18**  | Ein **Hardlink** auf das `aclocal`-Programm.                                                                             | Ermöglicht die explizite Nutzung einer bestimmten Automake-Version, falls mehrere Versionen installiert sind.                                                                       |
| **#automake-1.18** | Ein **Hardlink** auf das `automake`-Programm.                                                                            | Ermöglicht die explizite Nutzung einer bestimmten Automake-Version.                                                                                                                 |
