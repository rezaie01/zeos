## 📦 Intltool (Internationalization Tool)

---

### 1. Paketfunktion

- **Zweck:** Ein Werkzeug zur **Internationalisierung** (i18n).
    
- **Kern:** Extrahiert übersetzbare Text-Strings aus Quellcode-Dateien.
    
- **Relevanz:** Ermöglicht die Erstellung von Software, die **Native Language Support (NLS)** über Gettext bereitstellt.
    

---

### 2. Kompilierungsprozess im Überblick

- **Vorab-Korrektur (Perl-Kompatibilität):** Behebt eine Warnung, die durch Perl-Versionen 5.22 und höher verursacht wird (Bugfix im Quelltext).
    
    ```
    sed -i 's:\\\\\\\\${:\\\\\\\\\\{: intltool-update.in
    ```
    
- **Konfiguration:** Standardkonfiguration.
    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung und Test:**
    ```
    make
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Dokumentation:** Manuelle Installation der Internationalization (i18n) Anleitung.
    ```
    install -v -Dm644 doc/I18N-HOWTO /usr/share/doc/intltool-0.51.0/I18N-HOWTO
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm|Zweck (Crème de la Crème)|Nutzung (Extensiv für Systemintegration)|
|---|---|---|
|**#intltoolize**|**Initialisierung:** Bereitet ein Quellpaket für die Verwendung von Intltool vor.|**`intltoolize --force --copy`**: Stellt sicher, dass die benötigten Skripte und Makros in den Quellbaum kopiert werden.|
|**#intltool-update**|**Übersetzungskatalog-Verwaltung:** Aktualisiert die `po`-Dateien (Portable Object, die eigentlichen Übersetzungen).|**`intltool-update -m`**: **Merge**-Operation. Vergleicht alte Übersetzungen mit neuen extrahierten Zeichenketten und aktualisiert die `.po`-Dateien.|
|||**`intltool-update --pot`**: Erzeugt die **`POT`** (Portable Object Template)-Datei, die Vorlage für alle Übersetzungen.|
|**#intltool-extract**|**String-Extraktion:** Generiert temporäre Header-Dateien, die von **Gettext** gelesen werden können.|**`intltool-extract --type=gettext/glade datei.ext`**: Extrahiert übersetzbare Strings aus spezifischen Dateitypen (z.B. GTK-UI-Dateien, XML) und normalisiert sie für Gettext.|
|**#intltool-merge**|**Zusammenführung:** Fügt übersetzte Zeichenketten in die finalen Dateien (z.B. Konfigurationsdateien, Desktop-Dateien) ein.|**`intltool-merge --desktop datei.desktop.in datei.desktop`**: Ersetzt Platzhalter in der Vorlage (`.in`) durch die übersetzten Strings aus den `.po`-Dateien, um die finale Desktop-Datei zu erzeugen.|
|**#intltool-prepare**|**Vorbereitung:** Ein Hilfsskript, das vor dem **Merge**-Prozess aufgerufen wird.|**Wird intern verwendet:** Stellt sicher, dass die Zielverzeichnisse für die Übersetzungs-Merge-Dateien existieren.|