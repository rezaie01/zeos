## 📦 MPC: Kernwissen

### 1. Kernfunktion (Systemverständnis & Troubleshooting)

- **Zweck:** `MPC` ist eine Bibliothek zur **Arithmetik komplexer Zahlen** mit beliebiger Präzision und korrektem Runden. Es baut auf **`gmp`** und **`mpfr`** auf.
    
- **Relevanz:** Dieses Tool ist der **letzte Baustein** vor der Installation von **`GCC`** (dem GNU Compiler Collection). Es liefert die notwendigen komplexen mathematischen Funktionen, die der Compiler intern für einige seiner Berechnungen benötigt.
    

### 2. LFS-Relevanz (Toolchain-Kette)

- **Abhängigkeit:** `MPC` schließt die **kritische Kette der mathematischen Bibliotheken** ab: `gmp` $\rightarrow$ `MPFR` $\rightarrow$ **`MPC`**. Ein korrekter Build dieser drei Pakete ist **zwingend** erforderlich, um `GCC` erfolgreich zu kompilieren – das wichtigste **Core-Tool** in LFS.
    
- **Test-Suite:** Der **`make check`**-Schritt ist auch hier obligatorisch, da die korrekte Funktion von `MPC` direkt von der fehlerfreien Funktion von `gmp` und `mpfr` abhängt. Ein Testfehler an dieser Stelle bedeutet meist, dass Sie einen Fehler in den **vorherigen Paketen** beheben müssen (Ziel: **Troubleshooting üben**).
    

---

## 🛠️ Unbedingt zu kennende Kommandos (Bash-Fähigkeiten & Troubleshooting)

|**Kommando**|**Anwendung im LFS-Kontext**|**Ziel-Bezug**|
|---|---|---|
|**`make check`**|Führt die Test-Suite aus, um die Abhängigkeit zu `gmp` und `mpfr` zu verifizieren.|**Troubleshooting üben**|
|**`./configure --prefix=/usr`**|Der Standard-Konfigurationsschritt, der nun auf **drei** fehlerfrei gebaute Bibliotheken zurückgreift.|**Grundlegende Systemkonfiguration / Configure-Verständnis**|

**Kurz gesagt:** `MPC` ist der **finale mathematische Baustein** für den Compiler. Es ist der **Lackmustest** dafür, dass die gesamte **Präzisions-Kette (`gmp`, `mpfr`)** fehlerfrei installiert wurde, was für Ihr **Systemverständnis** und das Beherrschen des **Troubleshootings** entscheidend ist.