## 📦 Ncurses (New Curses)

---

### 1. Paketfunktion

- Enthält Bibliotheken für die **Terminal-unabhängige Handhabung von Zeichenbildschirmen**.
    
- Ermöglicht die Anzeige von **komplexem Text** und interaktiven Menüs auf dem Terminal (z.B. der Menü-Display während des Kernel-Baus).
    

---

### 2. Kompilierungsprozess im Überblick

- **Wichtige `configure`-Flags:**
    
    - `--with-shared`: Baut und installiert **Shared C Libraries**.
        
    - `--without-normal`: Verhindert die Installation **statischer C Libraries**.
        
    - `--with-cxx-shared`: Baut und installiert **Shared C++ Bindings**.
        
    - `--enable-pc-files`: Generiert **`.pc`-Dateien** für `pkgconf`.
        
    - `--with-pkg-config-libdir=/usr/lib/pkgconfig`: Stellt den korrekten Pfad für **pkg-config-Dateien** sicher.
        
- **Troubleshooting-Hinweis (Kritisch):** Die Installation kann die aktuell verwendete Shell zum Absturz bringen, da die Bibliothek `libncursesw.so.6.5` im laufenden Betrieb überschrieben wird.
    

---

### 3. Nach-der-Installation-Konfigurationen

- Installation unter Verwendung von `DESTDIR` zur Vermeidung eines Shell-Absturzes:
    
    Bash
    
    ```
    make DESTDIR=/ install
    ```
    
- Erstellung des Symlinks für die alte Bibliothek, um Kompatibilität zu gewährleisten:
    
    Bash
    
    ```
    ln -s libncursesw.so /usr/lib/libncurses.so
    ```
    
- Erstellung von Symlinks für Hilfsprogramme (`clear`, `reset`, `tabs`), um sie auf die installierten Terminfo-Einträge zu verlinken:
    
    Bash
    
    ```
    for file in /usr/bin/{clear,reset,tabs} ; do
        ln -sfv ../lib/terminfo/$(clear -x | cut -d/ -f2)/$(clear -x | cut -d/ -f3) $file
    done
    ```
    
    Bash
    
    ```
    unset file
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

|**Programm**|**Zweck (Crème de la Crème)**|**Ziel-Bezug (Systemverständnis / Bash-Fähigkeiten)**|
|---|---|---|
|**`libncursesw.so`**|Enthält Funktionen zur **komplexen Textanzeige** und zur Steuerung von Terminal-Fenstern.|**Systemverständnis:** Die Kernbibliothek für CLI-Anwendungen.|
|**`tic`**|**Terminfo-Compiler.** Übersetzt Terminfo-Dateien vom Quellformat in das Binärformat für `ncurses`.|**Grundlegende Konfiguration:** Wartung der Terminal-Fähigkeiten-Datenbank.|
|**`tput`**|Macht die Werte **Terminal-abhängiger Capabilities** für die Shell verfügbar.|**Bash-Fähigkeiten:** Wird in Skripten verwendet, um Terminal-Eigenschaften abzufragen und zu nutzen.|
|**`toe`**|Listet **alle verfügbaren Terminal-Typen** auf.|**Troubleshooting:** Schnelle Übersicht über unterstützte Terminal-Definitionen.|
|**`clear`**|Löscht den Bildschirm.|**Bash-Fähigkeiten:** Standardkommando zur Reinigung der Ansicht.|
|**`tset`**|Kann zur **Initialisierung** eines Terminals auf seine Standardwerte verwendet werden.|**Troubleshooting:** Zurücksetzen eines falsch konfigurierten Terminals.|	