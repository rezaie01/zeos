## 📦 Bc: Kernwissen

### 1. Kernfunktion (Systemverständnis & Bash-Fähigkeiten)

- **Zweck:** `bc` ist ein **Arbiter Precision Calculator** (Taschenrechner mit beliebiger Genauigkeit) und eine **numerische Verarbeitungssprache**. Es kann mit Fließkommazahlen arbeiten und ist genauer als die standardmäßigen Ganzzahlberechnungen der Shell.
    
- **Relevanz:** Es ist ein **Core-Tool** auf Unix-Systemen, essenziell für **Bash-Skripte** und die **Kommandozeile**, wenn Sie **Fließkomma-Arithmetik** (z.B. genaue Speicher- oder Netzwerkberechnungen) benötigen. Die Shell (`bash`) selbst kann nur mit Ganzzahlen rechnen.
    

### 2. LFS-Relevanz (Troubleshooting & Core-Tools)

- **Abhängigkeit:** Das `bc`-Paket ist von der **`readline`**-Bibliothek abhängig. Das Wissen, dass Sie die Option `-r` in der Konfiguration verwenden, um `readline` zu aktivieren, zeigt ein besseres Verständnis des **`configure`**-Schritts.
    
- **Wissen:** Das Ziel **"Bash-Fähigkeiten verbessern"** wird durch die Fähigkeit, komplexe Berechnungen direkt in Shell-Skripte einzubinden, deutlich gestärkt.
    

---

## 🛠️ Unbedingt zu kennende Kommandos (Bash-Fähigkeiten)

| **Kommando/Syntax**          | **Anwendung im LFS-Kontext**                                                              | **Ziel-Bezug**                                                                           |
| ---------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| **`bc -l`**                  | Startet `bc` mit der **Standard-Mathematik-Bibliothek** (ermöglicht z.B. Sinus, Cosinus). | **Bash-Fähigkeiten**                                                                     |
| **`echo "skala=3; 10/3"bc`** |                                                                                           | Berechnet 10 geteilt durch 3 mit 3 Nachkommastellen (**Pipes** und Variablen-Zuweisung). |
| **`bc`**                     | Das Hauptprogramm.                                                                        | **Core-Tools**                                                                           |
| **`dc`**                     | Der **Reverse-Polish Notation** (UPN) Rechner, der ebenfalls installiert wird.            | **Core-Tools (Optional):** Zeigt die Vielfalt der System-Utilities.                      |

**Kurz gesagt:** `bc` ist der **Präzisions-Rechner** der Kommandozeile. Sie lernen es als **Core-Tool** kennen und nutzen es in Verbindung mit **Pipes** und **Variablen**, um die Einschränkungen der Standard-Bash-Arithmetik zu überwinden.