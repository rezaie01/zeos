## 📦 Perl (Practical Extraction and Report Language)

---

### 1. Paketfunktion

- **Zweck:** Enthält die **Practical Extraction and Report Language** (Perl).
    
- **Kern:** Eine hochleistungsfähige, vielseitige Skriptsprache.
    
- **Charakteristik:** Kombiniert die besten Merkmale von **C, sed, awk und sh**.
    
- **Relevanz:** Essentiell für **Systemadministration, Berichterstattung** und die Kompilierung vieler anderer Linux-Pakete.
    

---

### 2. Kompilierungsprozess im Überblick

- **Systembibliotheken erzwingen (Wichtig):** Stellt sicher, dass die installierten Systembibliotheken für Zlib und Bzip2 verwendet werden, anstatt interne Kopien.
    ```
    export BUILD_ZLIB=False
    export BUILD_BZIP2=0
    ```
    
- **Konfiguration:** Volle Kontrolle über Pfade und Standardeinstellungen (`-des` für Default-Einstellungen).
    ```
    sh Configure -des \
        -D prefix=/usr \
        -D vendorprefix=/usr \
        -D privlib=/usr/lib/perl5/5.42/core_perl \
        -D archlib=/usr/lib/perl5/5.42/core_perl \
        -D sitelib=/usr/lib/perl5/5.42/site_perl \
        -D sitearch=/usr/lib/perl5/5.42/site_perl \
        -D vendorlib=/usr/lib/perl5/5.42/vendor_perl \
        -D vendorarch=/usr/lib/perl5/5.42/vendor_perl \
        -D man1dir=/usr/share/man/man1 \
        -D man3dir=/usr/share/man/man3
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):** Ausführung der umfassenden Tests.
    ```
    make test
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- Keine zusätzlichen Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm|Zweck (Crème de la Crème)|Nutzung (Extensiv für Systemintegration)|
|---|---|---|
|**#perl**|Der **Interpreter** der Programmier-/Skriptsprache.|**`perl skript.pl`**: Führt ein Perl-Skript aus.|
|||**`perl -e 'print "Hallo Welt\n"'`**: Führt Code **direkt** in der Kommandozeile aus (nützlich für schnelle Tests oder Pipelining).|
|||**`perl -pi -e 's/old/new/g' datei.txt`**: **In-Place-Bearbeitung** (`-i`) einer Datei. Führt eine **`sed`**-artige Such-und-Ersetzen-Operation durch (`s/old/new/g`).|
|||**`perl -w`**: Aktiviert **ausführliche Warnungen** (wichtig für robustes Skripting).|
|**#shasum**|Erstellt und prüft **SHA-Checksummen** (SHA-1, SHA-256, SHA-512).|**`shasum -a 256 datei`**: Generiert eine **SHA-256** Checksumme einer Datei.|
|||**`shasum -c checksum.txt`**: **Prüft** die Integrität von Dateien anhand einer gespeicherten Checksummen-Datei.|
|**#pod2man**|Konvertiert Perl-Dokumentation (im **POD-Format**) in das **Manpage-Format** (`*roff` Input).|**`pod2man modul.pm > modul.1`**: Generiert eine Manpage aus einem Perl-Modul.|
|**#pod2text**|Konvertiert POD-Daten in formatierten **ASCII-Text**.|**`pod2text skript.pl`**: Zeigt die eingebettete Dokumentation des Skripts in lesbarem Text an.|
|**#perldoc**|Zeigt ein Stück **Dokumentation** im POD-Format an, eingebettet in die Perl-Installation oder ein Perl-Skript.|**`perldoc Perl::Modul`**: Ruft die Dokumentation für ein installiertes Modul ab.|
|||**`perldoc -f print`**: Ruft die Dokumentation für eine **eingebaute Funktion** (`print`) ab.|
|**#prove**|**Test-Framework** zum Ausführen von Tests gegen das `Test::Harness`-Modul.|**`prove -l t/`**: Führt alle Testdateien im Verzeichnis `t/` aus.|
|||**`prove -v mein_test.t`**: Führt einen spezifischen Test im **ausführlichen Modus** (`-v`) aus.|
|**#sh**|Ein **Hardlink** auf den Perl-Interpreter.|Wird nur verwendet, wenn das System fälschlicherweise `sh` zu Perl auflöst (unüblich und nicht empfohlen).|