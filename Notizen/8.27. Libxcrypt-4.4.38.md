## 📦 Libxcrypt: Kernwissen

### 1. Kernfunktion (Systemverständnis & Sicherheit)

- **Zweck:** `Libxcrypt` ist eine moderne Bibliothek für das **Hashing von Passwörtern**. Es implementiert verschiedene, starke Hashing-Algorithmen (z.B. SHA-512, Blowfish) und ist der Nachfolger der veralteten Funktionen, die traditionell in der `glibc` enthalten waren.
    
- **Relevanz:** Dies ist **essenziell** für die **Benutzerverwaltung** und **Sicherheit** (Ihr Berufsziel). Wenn Sie später Benutzer erstellen (Ziel: **Grundlegende Systemkonfiguration**), wird die Passwort-Datei (`/etc/shadow`) diese Hashing-Funktionen verwenden, um die Passwörter sicher zu speichern.
    

### 2. LFS-Relevanz (Troubleshooting & Konfiguration)

- **Wissen:** Sie konfigurieren dieses Paket mit Optionen wie **`--enable-hashes=strong,glibc`** und **`--disable-obsolete-api=no`**. Dies zeigt Ihnen, wie Sie beim **`configure`**-Schritt die **Sicherheitsstandards** und die **Abwärtskompatibilität** Ihres Systems festlegen.
    
- **Troubleshooting:** Fehler bei der Passwort-Authentifizierung später im System können auf eine fehlerhafte oder unzureichend konfigurierte `Libxcrypt`-Installation zurückzuführen sein.

---
**Kurz gesagt:** `Libxcrypt` ist der Schlüssel zur **sicheren Passwortspeicherung** in Linux. Sie lernen dabei, wie Sie **Sicherheitsmerkmale konfigurieren** und **spezifische Bibliotheksdateien** in die korrekten Systempfade verschieben.