## 📦 Autoconf (Source Code Configuration)

---

### 1. Paketfunktion

- **Zweck:** Enthält Programme zur Erzeugung von **Shell-Skripten**, die Quellcode automatisch konfigurieren.
    
- **Kern:** Erlaubt es Software, sich an unterschiedliche **Unix-ähnliche Systeme** anzupassen.
    
- **Struktur:** Die generierten `configure`-Skripte laufen **unabhängig** von der installierten Autoconf-Version.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration.
    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite:**
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- Keine spezifischen Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

| Programm        | Zweck (Crème de la Crème)                                                                              | Nutzung (Extensiv für Systemintegration)                                                                                                                                                                                                 |
| --------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **#autoconf**   | Erzeugt das finale **`configure`** Shell-Skript.                                                       | **`autoconf`**: Liest die Eingabedatei `configure.ac` und generiert das lauffähige, **unabhängige** Konfigurationsskript `configure`.                                                                                                    |
| **#autoheader** | Erzeugt eine **Template-Header-Datei** (`config.h.in`) für C `#define`-Statements.                     | **`autoheader`**: Muss ausgeführt werden, nachdem **neue Überprüfungen** zu `configure.ac` hinzugefügt wurden, um die entsprechenden C-Header-Variablen vorzubereiten.                                                                   |
| **#autom4te**   | **M4-Makro-Wrapper**. Führt die Makroprozessierung von M4-Dateien (z.B. Autoconf-Skripte) durch.       | **Interne Verwendung:** Dient als **Grundlage** für alle Autoconf-Tools, um M4-Makros zu parsen und zu verarbeiten.                                                                                                                      |
| **#autoreconf** | **Automatisiert** den gesamten Build-System-Erstellungsprozess (inkl. Autoconf, Automake, Libtoolize). | **`autoreconf -vfi`**: **Standardbefehl** zum Aktualisieren der Build-Dateien nach Änderungen. Spart Zeit und gewährleistet die **korrekte Reihenfolge** der Werkzeuge (`-f` erzwingt die Neuerstellung, `-i` installiert Hilfsdateien). |
| **#autoscan**   | Hilft bei der **Initialerstellung** der `configure.ac`-Datei (`configure.scan`).                       | **`autoscan pfad/zum/quellcode`**: Sucht Quellcode nach **Portabilitätsproblemen** und benötigten System-Checks. Erzeugt einen Entwurf für das Build-System.                                                                             |
| **#autoupdate** | Aktualisiert veraltete Autoconf-Makronamen in einer `configure.in`-Datei.                              | **`autoupdate`**: Stellt die **Abwärtskompatibilität** sicher, wenn ältere Quellcodes mit einer neuen Autoconf-Version gebaut werden sollen.                                                                                             |
| **#ifnames**    | Listet alle **C-Präprozessor-Conditional-Identifier** auf, die ein Paket verwendet.                    | **`ifnames *.c`**: Dient als **Hilfsmittel** für Entwickler, um festzustellen, welche System-Header und Funktionen von `configure` überprüft werden müssen (z.B. ob `HAVE_SYS_TIME_H` benötigt wird).                                    |
