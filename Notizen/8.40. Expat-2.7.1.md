## 📦 Expat (XML Parser Library)

---

### 1. Paketfunktion

- **Zweck:** Enthält eine **streamorientierte C-Bibliothek** zum Parsen von XML.
    
- **Kern:** Eine schnelle, nicht-validierende Bibliothek zur Überprüfung, ob XML-Dokumente **wohlgeformt** (well-formed) sind.
    
- **Relevanz:** Essenzielle **Basisbibliothek** für viele Programme (z.B. KDE, GNOME, Firefox), die Konfigurationsdaten im XML-Format verarbeiten.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration.
    
    - `--disable-static`: Verhindert die Installation statischer Bibliotheken.
        
    - `--docdir`: Stellt den korrekten Pfad für die Dokumentation sicher.
        
    ```
    ./configure --prefix=/usr \
        --disable-static \
        --docdir=/usr/share/doc/expat-2.7.1
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):**
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Dokumentationsinstallation (Optional):** Manuelle Installation der HTML- und CSS-Dokumentation.
    ```
    install -v -m644 doc/*.html doc/*.css /usr/share/doc/expat-2.7.1
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm/Bibliothek|Zweck|
|---|---|
|**`libexpat.so`**|Die **XML-Parser-API-Bibliothek**.|
|**`xmlwf`**|Ein **Nicht-Validierungs-Dienstprogramm** zur Überprüfung der **Wohlgeformtheit** eines XML-Dokuments.|
|**`xmlwf datei.xml`**|**Überprüft die Syntax** einer XML-Datei. Erfolgreiche Prüfung gibt keinen Output und **Exit Code 0** zurück.|
|**`xmlwf -d [Verz.] datei.xml`**|**Dump-Modus** (`-d`). Speichert eine Kopie der geparsten XML-Struktur in einem Zielverzeichnis.|