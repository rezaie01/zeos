## 📦 acl: Kernwissen

### 1. Kernfunktion (Systemverständnis & Karriere)

- **Zweck:** `Acl` ermöglicht die Verwendung von **Access Control Lists** (ACLs) zur Festlegung **feingranularer Zugriffsrechte** auf Dateien und Verzeichnisse. Dies geht über die standardmäßigen Unix-Berechtigungen (Besitzer, Gruppe, Andere) hinaus.
    
- **Relevanz:** Dieses Tool ist **kritisch** für die **Sicherheit** und **Benutzerverwaltung** in modernen Linux-Umgebungen (Ihr Berufsziel). Es erlaubt Systemadministratoren, differenzierte Berechtigungen zu erteilen (z.B. einem einzelnen Benutzer spezielle Lese- oder Schreibrechte geben, ohne ihn zur Hauptgruppe hinzufügen zu müssen). Das Verständnis hierfür ist zentral für das Ziel **"Grundlegende Systemkonfiguration lernen"**.
    
- **Abhängigkeit:** `Acl` baut auf dem **`attr`**-Paket auf, das die erweiterten Attribute (`xattrs`) des Dateisystems bereitstellt.
    

### 2. LFS-Relevanz (Troubleshooting & Konfiguration)

- **Test-Suite-Hinweis:** Der Hinweis im LFS-Buch, dass ein Test fehlschlagen kann, weil **Coreutils** noch nicht mit ACL-Unterstützung gebaut wurde, ist ein **exzellentes Troubleshooting-Beispiel**. Es zeigt, wie Build-Fehler von der **Installationsreihenfolge** und den **Konfigurationsoptionen** anderer Pakete abhängen.
    

---

## 🛠️ Unbedingt zu kennende Kommandos (Bash-Fähigkeiten & Konfiguration)

|**Kommando**|**Zweck im LFS-Kontext**|**Ziel-Bezug**|
|---|---|---|
|**`getfacl datei`**|**Zeigt** die **ACLs** einer Datei an.|**Bash-Fähigkeiten & Grundlegende Konfiguration** (Prüfen von Berechtigungen)|
|**`setfacl -m u:benutzer:rw datei`**|**Setzt** eine ACL, um einem **spezifischen Benutzer** Lese- und Schreibrechte zu geben.|**Grundlegende Systemkonfiguration & Bash-Fähigkeiten** (ACLs manipulieren)|
|**`chacl datei`**|Ändert die **gesamte** Access Control List (veraltet, aber installiert).|**Core-Tools**|
|**`libacl.so`**|Die Bibliothek, die diese Funktionen bereitstellt.|**Systemverständnis** (Bibliotheken)|

**Kurz gesagt:** `Acl` ist der Schlüssel zur **feingranularen Berechtigungssteuerung** in Linux. Sie lernen damit, wie man **erweiterte Zugriffsrechte** konfiguriert und wie **Build-Abhängigkeiten** zwischen Basis-Tools (z.B. `Coreutils` und `Acl`) das **Troubleshooting** beeinflussen.