## 📦 Gperf (GNU gperf)

---

### 1. Paketfunktion

- **Zweck:** Generiert eine **perfekte Hash-Funktion** (Perfect Hash Function) aus einem Satz von Schlüsseln (Key Set).
    
- **Kern:** Eine perfekte Hash-Funktion ordnet jeden Schlüssel im Satz **ohne Kollisionen** einer eindeutigen Speicheradresse zu.
    
- **Relevanz:** Wird zur Erstellung von **effizienten Suchtabellen** (Lookup Tables) im Quellcode verwendet. Wichtiges **Entwickler-Tool** für die Toolchain (z.B. in GCC und Bash).
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration mit explizitem Pfad für die Dokumentation.
    ```
    ./configure --prefix=/usr --docdir=/usr/share/doc/gperf-3.3
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):**
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- Keine spezifischen Konfigurationsschritte nach der Installation erforderlich.
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm|Zweck|
|---|---|
|**`gperf`**|Der Generator der **perfekten Hash-Funktion**.|
|**`gperf datei.gperf`**|Liest Schlüssel aus der Eingabedatei (`.gperf`) und gibt den **generierten C/C++ Code** (Header-Datei) aus.|
|**`gperf -t -L C++ datei.gperf`**|Generiert C++-Code (`-L C++`), der zusätzlich eine Testfunktion (`-t`) für die erzeugte Hash-Tabelle enthält.|
|**`gperf -C`**|Generiert Code im ANSI C Stil (für moderne Compiler).|
