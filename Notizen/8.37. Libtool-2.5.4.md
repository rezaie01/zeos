## 📦 Libtool (GNU Libtool)

---

### 1. Paketfunktion

- **Zweck:** Enthält das GNU-Skript zur Unterstützung generischer Bibliotheken.
    
- **Kern:** Vereinfacht die Erstellung und Nutzung von **Shared Libraries** (z.B. `.so`-Dateien) durch eine konsistente, plattformübergreifende Schnittstelle. Es ist ein zentraler Bestandteil der GNU Autotools.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration ohne spezielle Optionen.
    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung:**
    ```
    make
    ```
    
- **Test-Suite (Verifizierung):**
    ```
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Bereinigung:** Entfernen der statischen Bibliothek `libltdl.a`.
    
- **Grund:** Diese Bibliothek wird nur für die interne Test-Suite von Libtool benötigt und im fertigen System nicht verwendet.
    ```
    rm -fv /usr/lib/libltdl.a
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm/Bibliothek|Zweck (Crème de la Crème)|Ziel-Bezug (Systemverständnis / Karriere)|
|---|---|---|
|**`libtool`**|Das Hauptskript. Stellt generalisierte Dienste für den **Bau von Shared Libraries** bereit und abstrahiert plattformspezifische Compiler-Flags.|**Systemverständnis:** Wird von `configure`-Skripten und `Makefiles` aufgerufen, um die Komplexität der Bibliothekerstellung zu verwalten.|
|**`libtoolize`**|Ein Dienstprogramm, das ein Paket (Quellcode) für die **Verwendung von `libtool` vorbereitet** (initialisiert).|**Karriere/Systemintegration:** Verständnis, wie Autotools-Projekte strukturiert und portabel gemacht werden.|
|**`libltdl.so`**|**Libtool Dynamic Loading library.** Eine Bibliothek, die die Komplexität des **dynamischen Ladens** von Shared Libraries (wie `dlopen()`) verbirgt.|**Systemverständnis:** Eine API, die es Programmen ermöglicht, zur Laufzeit (z.B. für Plugins) andere Bibliotheken zu öffnen.|