## 📦 Bash (Bourne-Again Shell)

---

### 1. Paketfunktion

- **Zweck:** Enthält die **Bourne-Again Shell**.
    
- **Kern:** Ein weit verbreiteter Befehlsinterpreter.
    
- **Funktion:** Führt **Expansionen und Substitutionen** auf Befehlszeilen aus, bevor sie ausgeführt werden, was es zu einem mächtigen Werkzeug macht.
    
- **Relevanz:** Die primäre Shell (Benutzerschnittstelle) für die Interaktion mit dem System.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration mit einer wichtigen Anpassung.
    ```
    ./configure --prefix=/usr \
        --without-bash-malloc \
        --with-installed-readline \
        --docdir=/usr/share/doc/bash-5.3
    ```
    
- **Wichtige `configure`-Flags:**
    
    - `--with-installed-readline`: Weist Bash an, die systemweit installierte `readline`-Bibliothek zu verwenden, anstatt die eigene interne Version.
        
- **Test-Suite (Wichtig für Troubleshooting):**
    
    - Die Tests erfordern einen nicht-privilegierten Benutzer (`tester`).
        
    - Die Tests müssen in einem **Pseudo-Terminal** laufen, was den Einsatz von `Expect` erfordert.
        
    ```
    LC_ALL=C.UTF-8 su -s /usr/bin/expect tester << "EOF"
    set timeout -1
    spawn make tests
    expect eof
    lassign [wait] __ value
    exit $value
    EOF
    ```
    
    - Bestimmte Tests (`run-builtins` oder locale-abhängige Tests) können fehlschlagen.
        

---

### 3. Nach-der-Installation-Konfigurationen

- **Installation:**
    ```
    make install
    ```
    
- **Aktivierung (Kritisch):** Unmittelbares Ersetzen der aktuell laufenden Shell durch die neu installierte Version.
    ```
    exec /usr/bin/bash --login
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung

|Programm|Zweck (Crème de la Crème)|Ziel-Bezug (Systemverständnis / Bash-Fähigkeiten)|
|---|---|---|
|**`bash`**|Der **Bourne-Again Shell**-Interpreter.|**Core-Tool:** Die primäre Schnittstelle zur Steuerung des Systems und zur Skriptausführung.|
|**`bash [skript.sh]`**|Führt ein Shell-Skript aus.|**Bash-Fähigkeiten:** Grundlage der Automatisierung.|
|**`bash --login`**|Startet eine **Login-Shell**.|**Grundlegende Konfiguration:** Lädt Konfigurationsdateien wie `/etc/profile` und `~/.bash_profile` (Systemstart-Verständnis).|
|**`sh`**|**Symbolischer Link** zu `bash`.|**Systemverständnis (Kompatibilität):** Wird für POSIX-konformes Verhalten aufgerufen.|
|**`sh [skript.sh]`**|Führt Skripte im **POSIX-kompatiblen Modus** aus (mimikt historisches `sh`-Verhalten).|**Troubleshooting:** Testen von Skript-Kompatibilität zwischen verschiedenen Shells.|
|**`bashbug`**|Ein Shell-Skript, das beim Erstellen und Versenden von Bug-Reports für `bash` hilft.|**Troubleshooting:** (Optional) Tool zur Meldung von Fehlern.|
