## 📦 DejaGNU: Kernwissen

### 1. Kernfunktion (Systemverständnis & Troubleshooting)

- **Zweck:** `DejaGNU` ist ein **Framework** zum Ausführen von **Test-Suiten** auf **GNU-Tools** (wie GCC, Binutils, GDB). Es stellt die Struktur und die notwendigen Wrapper bereit, um diese Tests konsistent und automatisch ablaufen zu lassen.
    
- **Relevanz:** Es ist ein **Entwickler-Tool**, das für die **Qualitätssicherung** von Compiler- und Debugging-Tools **entscheidend** ist.
    

### 2. LFS-Relevanz (Troubleshooting & Karriere)

- **Abhängigkeit:** `DejaGNU` ist in **`expect`** geschrieben, welches wiederum **`tcl`** verwendet. Dies demonstriert, wie die Basis-Tools in LFS aufeinander aufbauen, um komplexere Funktionalitäten zu ermöglichen.
    
- **Wissen:** Die Installation von `DejaGNU` ist der **letzte Schritt**, bevor Sie die wichtigen **Test-Suiten** (z.B. für GCC oder Binutils) laufen lassen können. Das korrekte Funktionieren dieses Pakets ist **essentiell** für Ihr Ziel **"Troubleshooting üben"**, da es die Werkzeuge liefert, um Fehler in Ihrem selbstgebauten Toolchain zu finden.
    
- **Wissen:** Die Installation in einem dedizierten **Build-Verzeichnis** (`mkdir build; cd build`) ist eine **Best Practice** im Linux-Umfeld, um das Quellverzeichnis sauber zu halten.
    

---

## 🛠️ Unbedingt zu kennende Kommandos (Bash-Fähigkeiten)

|**Kommando**|**Anwendung im LFS-Kontext**|**Ziel-Bezug**|
|---|---|---|
|**`runtest`**|Der Wrapper, der die `expect`-Shell lokalisiert und die Test-Suiten startet.|**Core-Tools & Troubleshooting:** Das primäre Kommando zum Starten der Tests in den nächsten Schritten.|
|**`dejagnu`**|Der **Kommando-Starter** des Frameworks.|**Core-Tools**|
|**`mkdir -v build; cd build`**|Erstellung eines separaten Build-Verzeichnisses.|**Bash-Fähigkeiten & Grundlegende Systemkonfiguration:** Wichtige Best Practice für das Kompilieren von Quellcode.|

**Kurz gesagt:** `DejaGNU` ist das **Test-Framework** für Ihre wichtigsten LFS-Tools. Sie bauen es, um die **Korrektheit** von GCC und Binutils zu **verifizieren**, was für das **Troubleshooting** des Toolchains unerlässlich ist.