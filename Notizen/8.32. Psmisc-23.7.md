## 📦 Psmisc (Process Miscellaneous)

---

### 1. Paketfunktion

- **Zweck:** Enthält Programme zur **Anzeige von Informationen** über laufende Prozesse.
    
- **Kern:** Stellt **zusätzliche und alternative Befehle** zu den Standard-Prozess-Tools bereit.
    
- **Relevanz:** Essentielles **Core-Tool** für die Systemdiagnose, Überwachung und das **Troubleshooting** von Prozessen.
    

---

### 2. Kompilierungsprozess im Überblick

- **Konfiguration:** Standardkonfiguration ohne spezielle Optionen.
    
    Bash
    
    ```
    ./configure --prefix=/usr
    ```
    
- **Kompilierung:**
    
    Bash
    
    ```
    make
    ```
    
- **Test-Suite (Optional):** Kann ausgeführt werden, ist aber kurz und dient der Verifikation.
    
    Bash
    
    ```
    make check
    ```
    
- **Installation:**
    
    Bash
    
    ```
    make install
    ```
    

---

### 3. Essenzielle Programme und ihre Nutzung

| **Programm**                        | **Zweck (Crème de la Crème)**                                                                       | **Ziel-Bezug (Troubleshooting / Bash-Fähigkeiten)**                                                                                 |
| ----------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **`pstree`**                        | Zeigt laufende Prozesse als **Hierarchiebaum** an.                                                  | **Systemverständnis:** Zeigt die **Eltern-Kind-Beziehungen** von Prozessen (z.B. welcher Dienst welche Sub-Prozesse gestartet hat). |
| **`pstree -p`**                     | Zeigt den Prozessbaum **mit PIDs** (Process IDs).                                                   | **Troubleshooting:** Schnelles Auffinden der PID eines Prozesses innerhalb seines Kontexts.                                         |
| **`killall`**                       | Beendet Prozesse **nach ihrem Namen**.                                                              | **Bash-Fähigkeiten / Systemkonfiguration:** Wichtiges Tool zum Beenden von Diensten oder hängenden Prozessen.                       |
| **`killall -s TERM [prozessname]`** | Sendet das **saubere Beenden-Signal** (`TERM`) an alle Instanzen des Prozesses.                     | **Grundlegende Konfiguration:** Kontrolliertes Herunterfahren von Diensten.                                                         |
| **`fuser`**                         | **Meldet PIDs** von Prozessen, die eine **bestimmte Datei oder ein Dateisystem** verwenden.         | **Troubleshooting:** Essentiell, um zu sehen, welche Prozesse eine Ressource (z.B. ein eingehängtes Laufwerk) blockieren.           |
| **`fuser -m /mnt/lfs`**             | Zeigt an, welche Prozesse das Einhängeverzeichnis `/mnt/lfs` verwenden (wichtig vor dem Aushängen). | **Bash-Fähigkeiten:** Praktische Anwendung für die Wartung von Dateisystemen.                                                       |
