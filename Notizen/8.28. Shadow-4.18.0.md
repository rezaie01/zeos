## 📦 Shadow: Kernwissen

### 1. Kernfunktion (Systemverständnis & Karriere)

- **Zweck:** `Shadow` ist das **Herzstück der Benutzer- und Gruppenverwaltung**. Es sichert die Benutzerauthentifizierung in Linux.
    
- **Das "Shadowing"-Konzept:** Das Paket trennt die **allgemeinen Benutzerinformationen** (in `/etc/passwd`, für alle lesbar) von den **sicheren, gehashten Passwörtern** (in `/etc/shadow`, nur für Root lesbar). Dieses Verständnis ist **fundamental** für das Ziel **"Systemkonfiguration lernen"** und die **"Karrierevorbereitung"**.
    
- **Abhängigkeit:** Es nutzt `Libxcrypt` (das Sie vorher gebaut haben), um die Passwörter zu hashen.
    

### 2. LFS-Relevanz (Troubleshooting & Konfiguration)

- **Wissen:** Die `configure`-Optionen (z.B. `--with-group-name-max-length=32`) zeigen, wie Sie **Systemgrenzen** festlegen.
    
- **Wissen:** Der `touch /usr/bin/passwd`-Befehl (aus dem LFS-Buch) ist ein **Troubleshooting**-Schritt, der verhindert, dass das Installationsskript eine falsche Annahme über den Dateipfad trifft.
    

---

## 🛠️ Unbedingt zu kennende Kommandos

Diese Kommandos sind **nicht optional** für Ihr Ziel als Fachinformatiker Systemintegration:

| Kommando            | Zweck                                                                        | Ziel-Bezug                                           |
| ------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------- |
| **`useradd`**       | Erstellt einen neuen Benutzer.                                               | **Grundlegende Konfiguration** (Basis-Skill)         |
| **`usermod`**       | Modifiziert einen Benutzer (z.B. zu Gruppen hinzufügen, Shell ändern).       | **Grundlegende Konfiguration**                       |
| **`userdel`**       | Löscht einen Benutzer.                                                       | **Grundlegende Konfiguration**                       |
| **`groupadd`**      | Erstellt eine neue Gruppe.                                                   | **Grundlegende Konfiguration**                       |
| **`passwd`**        | Ändert das Passwort eines Benutzers (oder einer Gruppe).                     | **Grundlegende Konfiguration** (Sicherheit)          |
| **`su`**            | Wechselt den Benutzer (Substitute User), meist zu **Root**.                  | **Bash-Fähigkeiten** (Essentiell für Administration) |
| **`pwck`**          | **Überprüft** die Integrität der `/etc/passwd`- und `/etc/shadow`-Dateien.   | **Troubleshooting üben** (Systemintegrität prüfen)   |
| **`grpck`**         | **Überprüft** die Integrität der `/etc/group`- und `/etc/gshadow`-Dateien.   | **Troubleshooting üben**                             |
| **`nologin`**       | Eine Shell, die den Login verhindert (für System- oder deaktivierte Konten). | **Systemverständnis** (Sicherheit)                   |
| **`vipw` / `vigr`** | **Editiert** die Konfigurationsdateien **sicher** (mit Dateisperre).         | **Karrierevorbereitung** (Best Practice)             |
