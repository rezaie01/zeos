#Python #python3 #pip3 #libpython3.13.so #libpython3.so #2to3 #pydoc3 #idle3

## 🛠️ Python-3.13.7: Lernnotizen für LFS

### 1. Paketfunktion

- Enthält die **Python-Entwicklungsumgebung**.
    
- Interpretierte Computersprache.
    
- Nützlich für objektorientierte Programmierung, das Schreiben von Skripten und das Prototyping von Anwendungen.
    

---

### 2. Kompilierungsprozess

#### **Vorbereitung**

- Konfiguration des Pakets:
    
    Bash
    
    ````
    ./configure --prefix=/usr \
                --enable-shared \
                --with-system-expat \
                --enable-optimizations \
                --without-static-libpython
    ``` [cite: 46, 47, 48, 49, 50]
    ````
    
- **Wichtigste Optionen:**
    
    - `--with-system-expat`: Linkt gegen die Systemversion von **Expat**.
        
    - `--enable-optimizations`: Aktiviert umfangreiche Optimierungsschritte; der Interpreter wird **zweimal** gebaut.
        

#### **Kompilierung und Test**

- Kompilierung: `make`
    
- Testdurchführung mit Zeitlimit (zur Vermeidung von Hängern):
    
    Bash
    
    ````
    make test TESTOPTS="--timeout 120"
    ``` [cite: 60]
    * **Troubleshooting:** Bei langsamen Systemen ggf. `TESTOPTS` erhöhen[cite: 61].
    * **Troubleshooting:** Fehlgeschlagene Tests werden automatisch erneut ausgeführt und gelten als bestanden, wenn der erneute Lauf erfolgreich ist[cite: 62, 63].
    * **Bekannter Fehler:** Test `test_ssl` schlägt in der `chroot`-Umgebung bekanntermaßen fehl[cite: 64].
    
    ````
    

#### **Installation**

- Installation des Pakets: `make install`
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Systemweite `pip3`-Nutzung:** LFS verwendet `pip3` als **`root`** zur systemweiten Installation von Python-Modulen.
    
- **Wichtiger Punkt:** Module müssen als `root` installiert werden, um für alle Benutzer zugänglich zu sein.
    
- **Warnungsunterdrückung:** `pip3` löst Warnungen aus (wegen root-Nutzung und fehlender Versionsprüfung im chroot).
    
- Konfigurationsdatei zur Unterdrückung der Warnungen:
    
    Bash
    
    ```
    cat > /etc/pip.conf << EOF
    [global]
    root-user-action = ignore
    disable-pip-version-check = true
    EOF
    ``` 

#### **Optionale Dokumentationsinstallation**

- Installation der vorformatierten HTML-Dokumentation (Tarball wird als in `/usr/src` liegend angenommen):
    ```
    install -v -dm755 /usr/share/doc/python-3.13.7/html
    tar --strip-components=1 \
        --no-same-owner \
        --no-same-permissions \
        -C /usr/share/doc/python-3.13.7/html \
        -xvf ../python-3.13.7-docs-html.tar.bz2
    ```
* `--no-same-owner` und `--no-same-permissions`: Stellt korrekte Dateiberechtigungen für das LFS-System sicher.
    

---

### 4. Essenzielle Programme und Bibliotheken

| Programm / Bibliothek | Beschreibung                                       | Nutzung (Fokus: Systemintegration)                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------- | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #python3              | Der **Python-Interpreter**.                        | Führt Python-Skripte aus (`python3 <skript.py>`).<br><br>  <br><br>**System-Automatisierung:** Zentrales Werkzeug für Systemverwaltungs-Skripte (Monitoring, Backup, Konfiguration).                                                                                                                                                                                                                                                |
| #libpython3.13.so     | Die dynamische Python-Laufzeitbibliothek.          | Wird von Programmen geladen, um den **Interpreter einzubetten** und Python-Code direkt auszuführen (Embedding).                                                                                                                                                                                                                                                                                                                     |
| #pip3                 | Der **Paketinstallationsmanager** für Python.      | Installiert Python-Pakete systemweit (als `root`) oder in virtuellen Umgebungen.<br><br>  <br>  <br><br>**Wichtige Kommandos:**<br><br>  <br><br>* **Installation:** `pip3 install <paket>`<br><br>  <br><br>* **Upgrade:** `pip3 install --upgrade <paket>` (Aktualisiert ein Modul)<br><br>  <br><br>* **Neuinstallation:** `pip3 install --force-reinstall-no-deps <paket>` (Zum Downgrade oder zur erzwungenen Neuinstallation) |
| #2to3                 | Konvertierungsprogramm.                            | Liest Python 2.x Quellcode und transformiert ihn in gültigen **Python 3.x Code**.                                                                                                                                                                                                                                                                                                                                                   |
| #pydoc3               | Python-Dokumentationswerkzeug.                     | Zeigt die Dokumentation für Module, Funktionen, Klassen und Methoden direkt in der Shell an.                                                                                                                                                                                                                                                                                                                                        |
| #python3-config       | Skript zur Bereitstellung von Build-Informationen. | Wird von anderen Paketen verwendet, um Kompilierungs-Flags (`CFLAGS`, `LDFLAGS`) für die Verknüpfung mit der Python-Installation zu ermitteln.                                                                                                                                                                                                                                                                                      |