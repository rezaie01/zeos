## 📦 Inetutils (Internet Utilities)

---

### 1. Paketfunktion

- **Zweck:** Enthält grundlegende Programme für die **Netzwerkkonfiguration** und **Netzwerkdiagnose**.
    
- **Kern:** Bietet klassische UNIX-Netzwerk-Utilities wie `ping`, `ifconfig` und `ftp`.
    

---

### 2. Kompilierungsprozess im Überblick

- **Patch (GCC-Kompatibilität):**
    ```
    sed -i 's/def HAVE_TERMCAP_TGETENT/1/ telnet/telnet.c
    ```
    
- **Wichtige `configure`-Flags (Fokus auf Sicherheit/Struktur):**
    
    - **Standort-Anpassung:** `--bindir=/usr/bin` (Programme im primären Binärpfad), `--localstatedir=/var` (variabler Zustand).
        
    - **Deaktivierung (Sicherheit/Veraltet):**
        
        - `--disable-logger` (Util-linux hat neuere Version).
            
        - `--disable-whois` (Veraltet).
            
        - `--disable-r*`: Deaktiviert die **unsicheren, veralteten** Remote-Befehle (`rcp`, `rexec`, `rlogin`, `rsh`).
            
        - `--disable-servers`: Deaktiviert alle mitgelieferten Netzwerk-Server.
            
    ```
    ./configure --prefix=/usr \
        --bindir=/usr/bin \
        --localstatedir=/var \
        --disable-logger \
        --disable-whois \
        --disable-rcp \
        --disable-rexec \
        --disable-rlogin \
        --disable-rsh \
        --disable-servers
    ```
    
- **Kompilierung und Test:**
    ```
    make
    make check
    ```
    
- **Installation:**
    ```
    make install
    ```
    

---

### 3. Nach-der-Installation-Konfigurationen

- **Pfadkorrektur:** Verschiebung des Verwaltungs-Tools `ifconfig` an den Standardort für administrative Binärdateien.
    ```
    mv -v /usr/bin/ifconfig /usr/sbin/ifconfig
    ```
    

---

### 4. Essenzielle Programme und ihre Nutzung
#ITAndLinux

|Programm|Zweck (Crème de la Crème)|Nutzung (Erweiterte Beispiele)|
|---|---|---|
|**`ping`**|**Diagnostik:** Überprüfung der **Host-Erreichbarkeit** und der **Netzwerklatenz** (ICMP-Echo).|**`ping -c 5 192.168.1.1`**: Sendet **5 Pakete** und stoppt (für Skripte).|
|||**`ping -i 0.2 server.local`**: Sendet Pakete im **0,2-Sekunden-Intervall** (für schnelle Latenzüberwachung).|
|||**`ping -W 10 ziel-ip`**: Setzt das **Timeout** auf 10 Sekunden pro Paket.|
|**`ifconfig`**|**Netzwerkschnittstellen-Management** (Konfiguration/Status).|**`ifconfig eth0 up`**: **Aktiviert** eine Schnittstelle.|
|||**`ifconfig eth0 down`**: **Deaktiviert** eine Schnittstelle.|
|||**`ifconfig eth0 192.168.1.1 netmask 255.255.255.0`**: **Setzt** IP-Adresse und Subnetzmaske (temporär).|
|||**`ifconfig -a`**: Zeigt den Status **aller** Schnittstellen (auch inaktive) an.|
|**`traceroute`**|**Netzwerkanalyse:** Verfolgt den **Paketpfad** und zeigt **alle Router (Hops)** mit Latenz an.|**`traceroute ziel-host`**: Standardpfadverfolgung.|
|||**`traceroute -m 10 ziel-host`**: Begrenzt die maximale Anzahl von **Hops auf 10**.|
|||**`traceroute -n ziel-host`**: Unterdrückt die **DNS-Auflösung** der Hops (schnellere Ergebnisse).|
|**`telnet`**|**Diagnostik/Port-Check:** Stellt **rohe TCP-Verbindungen** her (unsicher für Login).|**`telnet mailserver.com 25`**: Stellt Verbindung zum **SMTP-Port 25** her (Prüfen, ob Dienst läuft).|
|||**`telnet 10.0.0.1 80`**: Prüft die Erreichbarkeit des **Web-Ports 80** auf der internen IP.|
|**`ftp`**|**Dateiübertragung** (File Transfer Protocol) Client.|**`ftp ziel-host`**: Startet eine **interaktive Sitzung** (Login erforderlich).|
|||**Innerhalb von FTP:** `get dateiname` (Datei herunterladen), `put dateiname` (Datei hochladen).|
|**`tftp`**|**Trivial File Transfer Protocol** Client (einfache, unauthentifizierte Übertragung).|**`tftp -g -r remote_datei -l local_datei ziel-ip`**: **Holt** (`-g`) die **remote_datei** (`-r`) und speichert sie lokal als **local_datei** (`-l`).|
|||**`tftp -p -l local_datei -r remote_datei ziel-ip`**: **Sendet** (`-p`) die lokale Datei.|